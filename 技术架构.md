# 技术架构文档

> Planner App 技术架构详细说明

---

## 📐 总体架构

```
┌─────────────────────────────────────────────────────────┐
│                     Client Layer                         │
├─────────────────────────────────────────────────────────┤
│  Mobile (uni-app)  │  Web (React/Vue)  │  Desktop (Tauri)  │
└─────────────────────────────────────────────────────────┘
                            ↓ HTTPS
┌─────────────────────────────────────────────────────────┐
│                  API Gateway / Load Balancer            │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│              Application Layer (FastAPI)                 │
├─────────────────────────────────────────────────────────┤
│  Auth Middleware  │  Rate Limiting  │  Logging          │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                  Business Logic Layer                   │
├─────────────────────────────────────────────────────────┤
│  Task Service  │  Event Service  │  Habit Service      │
│  Notification Service  │  Statistics Service            │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                    Data Access Layer                    │
├─────────────────────────────────────────────────────────┤
│              SQLAlchemy ORM (Models/Queries)           │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   Data Storage Layer                    │
├─────────────────────────────────────────────────────────┤
│    SQLite (Dev)  │  PostgreSQL (Prod)  │  Redis        │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                  Background Services                    │
├─────────────────────────────────────────────────────────┤
│        APScheduler (Tasks)  │  Worker Queue              │
└─────────────────────────────────────────────────────────┘
```

---

## 🔧 技术栈选型

### 后端技术栈

| 技术 | 版本 | 用途 | 说明 |
|------|------|------|------|
| Python | 3.11+ | 编程语言 | 现代特性，性能优秀 |
| FastAPI | 0.104+ | Web 框架 | 高性能、异步、自动文档 |
| SQLAlchemy | 2.0+ | ORM | 数据库抽象，类型安全 |
| Pydantic | 2.0+ | 数据验证 | 请求/响应验证 |
| Uvicorn | 0.24+ | ASGI 服务器 | 高性能异步服务器 |
| APScheduler | 3.10+ | 任务调度 | 定时任务，提醒功能 |
| Redis | 7.0+ | 缓存/队列 | 缓存、消息队列 |
| PostgreSQL | 15+ | 生产数据库 | 高性能、ACID 事务 |
| SQLite | 3.40+ | 开发数据库 | 轻量级，零配置 |

### 前端技术栈

| 技术 | 版本 | 用途 | 说明 |
|------|------|------|------|
| uni-app | 3.x | 移动端开发 | 跨平台、多端统一 |
| React | 18+ | Web 开发 | 生态丰富、组件化 |
| Vue.js | 3.4+ | Web 开发 | 渐进式框架 |
| Tailwind CSS | 3.4+ | CSS 框架 | 快速样式开发 |
| shadcn/ui | Latest | UI 组件库 | 美观、可定制 |

### 开发工具

| 工具 | 用途 | 说明 |
|------|------|------|
| Poetry | 依赖管理 | Python 项目依赖 |
| Git | 版本控制 | 代码版本管理 |
| Docker | 容器化 | 部署和环境一致性 |
| GitHub Actions | CI/CD | 自动化测试和部署 |
| Postman | API 测试 | 接口调试和文档 |

---

## 📁 项目结构

```
planner-app/
├── backend/                    # 后端项目
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py            # FastAPI 应用入口
│   │   ├── config.py          # 配置文件
│   │   ├── database.py        # 数据库配置
│   │   ├── models/            # SQLAlchemy 模型
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── task.py
│   │   │   ├── event.py
│   │   │   ├── habit.py
│   │   │   └── ...
│   │   ├── schemas/           # Pydantic 模型
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── task.py
│   │   │   └── ...
│   │   ├── api/               # API 路由
│   │   │   ├── __init__.py
│   │   │   ├── auth.py
│   │   │   ├── tasks.py
│   │   │   ├── events.py
│   │   │   ├── habits.py
│   │   │   └── ...
│   │   ├── services/          # 业务逻辑层
│   │   │   ├── __init__.py
│   │   │   ├── task_service.py
│   │   │   ├── event_service.py
│   │   │   ├── habit_service.py
│   │   │   └── ...
│   │   ├── core/              # 核心功能
│   │   │   ├── __init__.py
│   │   │   ├── auth.py        # 认证
│   │   │   ├── security.py    # 安全
│   │   │   └── deps.py        # 依赖注入
│   │   └── utils/             # 工具函数
│   │       ├── __init__.py
│   │       ├── scheduler.py   # 任务调度器
│   │       └── notification.py
│   ├── tests/                 # 测试
│   ├── alembic/               # 数据库迁移
│   ├── requirements.txt
│   ├── pyproject.toml
│   └── Dockerfile
├── frontend/                   # 前端项目
│   ├── mobile/                # uni-app 移动端
│   │   ├── src/
│   │   │   ├── App.vue
│   │   │   ├── main.js
│   │   │   ├── pages.json
│   │   │   ├── manifest.json
│   │   │   ├── pages/
│   │   │   └── components/
│   │   ├── package.json
│   │   └── vite.config.js
│   └── web/                   # React/Vue Web 端
│       ├── src/
│       ├── package.json
│       └── vite.config.js
├── docs/                      # 文档
│   ├── 功能清单.md
│   ├── API文档.md
│   └── 部署文档.md
└── README.md
```

---

## 🔐 认证与授权

### JWT 认证流程

```
1. 用户登录
   ↓
2. 验证用户名/密码
   ↓
3. 生成 Access Token (15分钟) + Refresh Token (7天)
   ↓
4. 返回 Token 给客户端
   ↓
5. 客户端存储 Token (LocalStorage/SecureStorage)
   ↓
6. 每次请求携带 Access Token (Authorization: Bearer <token>)
   ↓
7. 服务端验证 Token 签名和过期时间
   ↓
8. Access Token 过期时使用 Refresh Token 刷新
```

### 安全措施

| 措施 | 说明 |
|------|------|
| 密码哈希 | 使用 bcrypt 哈希存储 |
| HTTPS | 生产环境强制 HTTPS |
| CORS | 配置允许的域名 |
| Rate Limiting | API 请求频率限制 |
| Input Validation | 使用 Pydantic 验证输入 |
| SQL Injection | 使用 ORM 防止注入 |
| XSS | 前端输出转义 |

---

## 🗄️ 数据库设计

### 数据库选型

**开发环境**: SQLite
- 零配置，文件即数据库
- 适合快速开发和测试

**生产环境**: PostgreSQL
- 高性能，支持复杂查询
- ACID 事务支持
- 高可用和复制支持

### 数据迁移

使用 Alembic 进行数据库迁移：

```bash
# 创建迁移脚本
alembic revision --autogenerate -m "Add habit_check_ins table"

# 执行迁移
alembic upgrade head

# 回滚迁移
alembic downgrade -1
```

---

## ⚡ 性能优化

### 缓存策略

```python
# Redis 缓存示例
from functools import wraps
import redis

redis_client = redis.Redis(host='localhost', port=6379, db=0)

def cache(ttl=300):
    def decorator(func):
        @wraps(func)
        async def wrapper(*args, **kwargs):
            cache_key = f"{func.__name__}:{str(args)}:{str(kwargs)}"
            cached = redis_client.get(cache_key)
            if cached:
                return json.loads(cached)
            result = await func(*args, **kwargs)
            redis_client.setex(cache_key, ttl, json.dumps(result))
            return result
        return wrapper
    return decorator
```

### 数据库优化

| 优化项 | 说明 |
|--------|------|
| 索引 | 为查询字段添加索引 |
| 分页 | 使用 LIMIT/OFFSET 分页 |
| 懒加载 | 避免关联查询 N+1 问题 |
| 连接池 | 使用数据库连接池 |
| 查询优化 | 使用 EXPLAIN 分析查询 |

---

## 🔔 任务调度

### APScheduler 配置

```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler

scheduler = AsyncIOScheduler()

# 任务提醒
@scheduler.scheduled_job('cron', hour=9, minute=0, id='daily_task_reminder')
async def daily_task_reminder():
    # 查询今日到期的任务
    # 发送提醒通知
    pass

# 习惯打卡提醒
@scheduler.scheduled_job('cron', hour=22, minute=0, id='habit_check_reminder')
async def habit_check_reminder():
    # 查询今日未打卡的习惯
    # 发送漏打卡提醒
    pass

scheduler.start()
```

---

## 📊 监控与日志

### 日志配置

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('app.log'),
        logging.StreamHandler()
    ]
)

logger = logging.getLogger(__name__)
```

### 监控指标

| 指标 | 说明 |
|------|------|
| 请求响应时间 | API 响应时间监控 |
| 错误率 | 5xx 错误率统计 |
| 并发连接数 | 当前活跃连接数 |
| 数据库连接池 | 连接池使用情况 |
| 缓存命中率 | Redis 缓存效率 |

---

## 🚢 部署方案

### Docker 部署

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Docker Compose

```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://user:pass@db:5432/planner
      - REDIS_URL=redis://redis:6379
    depends_on:
      - db
      - redis

  db:
    image: postgres:15
    environment:
      - POSTGRES_DB=planner
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass

  redis:
    image: redis:7
```

---

## 🔐 安全最佳实践

1. **环境变量管理**
   - 使用 `.env` 文件存储敏感信息
   - 生产环境使用密钥管理服务

2. **API 安全**
   - 所有 API 都需要认证（除登录/注册）
   - 使用 HTTPS
   - 限制 API 请求频率

3. **数据安全**
   - 敏感数据加密存储
   - 定期备份数据库
   - 访问日志记录

4. **代码安全**
   - 定期更新依赖
   - 使用安全扫描工具
   - Code Review

---

## 📈 扩展性考虑

### 水平扩展

```
Load Balancer
    ↓
┌─────────┬─────────┬─────────┐
│ App 1   │ App 2   │ App 3   │
└─────────┴─────────┴─────────┘
    ↓         ↓         ↓
┌───────────────────────────┐
│      PostgreSQL (主)      │
│   + 读写分离 (从库)       │
└───────────────────────────┘
```

### 微服务化（未来）

```
┌──────────┬──────────┬──────────┐
│ Auth     │ Task     │ Event    │
│ Service  │ Service  │ Service  │
└──────────┴──────────┴──────────┘
         ↓ API Gateway
    ┌──────────────┐
    │    Client    │
    └──────────────┘
```

---

## 📝 开发规范

### 代码规范

- 遵循 PEP 8
- 使用 Type Hints
- 编写 Docstring
- 单元测试覆盖率 > 80%

### Git 工作流

```
main (生产)
  ↑
develop (开发)
  ↑
feature/* (功能分支)
```

### Commit Message 格式

```
feat: 添加任务看板功能
fix: 修复日程提醒不生效的问题
docs: 更新 API 文档
style: 格式化代码
refactor: 重构任务服务
test: 添加单元测试
chore: 更新依赖
```

---

**文档版本**: v1.0
**最后更新**: 2026-02-14
